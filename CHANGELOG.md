# 更新记录

## 未发布 - 2026-08-27

- 内部包名统一为 `omniisle-write`，与 GitHub 仓库名和英文项目名保持一致；产品名称仍为“知屿写作 Omniisle Write”。
- 增加 5 个随社区版发布的知屿内置提示词模板：长篇小说大纲、单章细纲、拆书 A、拆书 B 和番茄向正文；首次启动只补缺失项，不按标题覆盖用户模板。
- 操作教程改用同一套公开模板，不再依赖线上模板种子或第三方拆书提示词；“番茄向”明确标注为非平台官方且不承诺成绩。
- 恢复正文生成的“剧情 / 字数”双模式按钮、状态和使用说明。
- 剧情模式一次生成并以完整收束为优先；字数模式按计划续写并以目标字数为优先。
- 两种模式均只直连用户当前选择的自备 API，不依赖知屿登录、计费或后端任务。
- 单章目标限制为 20000 字；高于 5 次 API 请求的计划会在开始前显示预计次数并二次确认。
- 执行日志保留真实进度和模型错误，同时继续隐藏 API Key 等敏感内容。
- 增加假模型执行器回归，覆盖请求次数、达标停止、模型冻结、完成标记、不完整草稿、401、空响应、网络边界和日志格式。
- “添加模型”支持根据基础 URL 和 API Key 检索可用模型并下拉选择；服务商不支持列表或浏览器拦截跨域时，保留手动模型 ID 兼容入口。同名模型会安全更新原配置，取消检索会中止请求并清空表单。
- MiniMax 配置区分国内站与国际站，分别填写官方对应地址；401/403 会提示检查 Key 所属站点，避免把站点不匹配误报为普通权限问题。
- 增加只监听 `127.0.0.1` 的本机模型转发通道，支持未开放浏览器跨域的用户自备 API，同时保持流式生成、取消、超时和真实上游状态；会话令牌、同源校验、请求头白名单、地址限制和不落日志测试已补齐。
- 添加 OpenCode Zen 正文兼容选项，只展示其 `chat/completions` 模型族；基础 URL 误填完整 `/models` 地址时会自动纠正，避免正文被拼到 `/models/chat/completions`。
- 所有社区版 AI 输出统一增加简体中文约束，并在共享输出层过滤思考标签、英文开场和后段英文正文；“知屿助手 / 内容反馈”已由用户使用自备模型完成实际页面中文输出验收。

## 0.1.0-alpha.1 - 2026-08-23

- 移除社区运行链中的知屿登录、支付、积分、会员和云任务依赖。
- 保留正文生成、改写、润色、大纲、剧本与全文分析，统一使用用户自备 API。
- 全文分析改为本机检查点、八份资料保存和分阶段恢复。
- 历史版本、模板、章节和作品管理改为本机保存；用户提示词不上传。
- 增加旧后端、额外联网、动态缺失素材和核心本机流程门禁。
- 完成最新构建、核心冒烟、全文分析、API Key 并发、旧 Key 升级、许可证、安全审计和 Chrome/Edge 本机验收，技术状态更新为 GO。

## 公开候选准备 - 2026-08-22

- 建立不继承旧 Git 历史的社区版公开候选目录。
- 通过白名单迁入本地写作、用户自备模型和全文分析代码。
- 把测试期文件名整理为正式源码名。
- 加入本地网络限制、公开边界审计和第三方许可证副本。
- 确定个人权利主体 Zeyu、英文名称 Omniisle Write、`AGPL-3.0-only`、CLA 与独立素材许可边界。
- 此日快照仍为 NO-GO；后续状态见 2026-08-23 记录。
