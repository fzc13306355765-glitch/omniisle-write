# 公开状态

截至 2026-08-23，本目录的技术状态为 **GO**：可以作为知屿写作 Omniisle Write 的 GitHub 社区版源码仓库使用。

## 已确认边界

- 个人权利主体和公开署名为 Zeyu；项目英文名称为 Omniisle Write。
- 项目自身代码采用 `AGPL-3.0-only`，保留另行商业许可；外部贡献采用 CLA。
- 用户上传的提示词、作品、图片和其他内容不属于公开项目，不会作为仓库内容上传。
- 社区版不提供知屿后端、登录、云存储、计费、支付、管理后台或托管模型。
- 正文生成、改写、润色、大纲、剧本和全文分析使用用户自己配置并确认的模型 API。
- 作品、章节、模板、历史版本与全文分析检查点保存在当前浏览器。

## 2026-08-23 验证证据

- 构建检查通过：176 个社区脚本、17 个社区样式，运行包指纹 `8be4cd7aded1`。
- 核心本机冒烟通过：写作入口、历史版本、模板隐私和自备 API 边界均通过。
- 全文分析冒烟通过：自备模型、本机检查点、八份资料生成和分阶段恢复均通过。
- 公开审计通过：禁止路径、治理文件、服务端/商业代码、疑似密钥、大文件、素材权属记录、缺失文件、额外联网全部为零阻断。
- 本机静态服务验证通过：首页、运行脚本、样式、Logo、壁纸均返回 200；不允许公开提供的 `package.json` 返回 404。

## 仍需如实说明

- 本仓库是源码项目，不包含 Windows 安装包、自动更新、知屿服务器或免费模型额度。
- AI 是否可用、价格和隐私规则由用户选择的模型供应商决定。
- Logo 和山水壁纸的来源说明基于 Zeyu 的权利声明；生成记录需要由权利人私下持续留存，本技术审查不能代替法律鉴定。
- 当前已有静态和脚本冒烟，但尚未配置 GitHub CI 和覆盖 Chrome、Edge 全流程的自动化测试；这不阻断首次源码公开，后续应补齐。

每次公开提交前仍应运行：

```bash
npm test
npm run audit:public
```
