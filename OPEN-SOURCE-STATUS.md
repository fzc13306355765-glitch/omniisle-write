# 公开状态

截至 2026-08-27，本目录的公开状态为 **GO（GitHub 已公开）**。许可证、安全、自动检查、浏览器、核心功能和 Private 候选版真人验收均已通过；仓库 `fzc13306355765-glitch/omniisle-write` 已由维护者明确确认并设置为 Public。

## 已确认边界

- 个人权利主体和公开署名为 Zeyu；项目英文名称为 Omniisle Write。
- 项目自身代码采用 `AGPL-3.0-only`，保留另行商业许可；外部贡献采用 CLA。
- 用户上传的提示词、作品、图片和其他内容不属于公开项目，不会作为仓库内容上传。
- 社区版不提供知屿后端、登录、云存储、计费、支付、管理后台或托管模型。
- 正文生成、改写、润色、大纲、剧本和全文分析使用用户自己配置并确认的模型 API。
- 正文生成已恢复剧情/字数双模式：剧情模式一次生成并检查完整收束，字数模式按计划续写；两种模式均不调用知屿登录、计费或后端任务。
- 单章目标上限为 20000 字；字数模式预计超过 5 次用户 API 请求时，会在调用前明确显示预计次数并要求二次确认。
- 添加模型可根据用户填写并确认的基础 URL 和 API Key 检索 `/models` 列表，再从去重后的模型中选择；本机运行时由仅监听 `127.0.0.1` 的转发通道兼容未开放浏览器跨域的服务，纯静态部署仍只支持允许浏览器直连的提供商。
- 社区版首次启动会向本机模板库补入 5 个知屿公开模板；初始化按稳定 ID 幂等执行，不按标题匹配，也不会覆盖用户创建或导入的提示词。
- 作品、章节、模板、历史版本与全文分析检查点保存在当前浏览器。

## 已关闭的发布门禁

- LangExtract 的 Apache-2.0 全文、Google 归属、来源与修改说明已保留；运行包不会删除法律声明。
- `THIRD_PARTY_NOTICES.md` 与 `package-lock.json` 精确对应 83 项依赖，包含运行、构建、传递和可选平台依赖及许可证正文。
- API Key 默认只在当前页面会话保存；用户主动开启后才加密持久化。旧配置兼容、开关并发和旧明文安全升级均有自动测试。
- 模型列表与正文请求只通过统一 AI 请求模块发起；API Key 不进入网址和执行日志。本机转发要求同源随机令牌、请求头白名单，拒绝重定向和不安全地址；外部地址必须使用 HTTPS，本机模型仅额外允许经过确认的 localhost HTTP。
- GitHub Actions、Dependabot、许可证检查和密钥检查均为免费配置；Actions 已固定到 GitHub 官方 `v7` 完整提交指纹，并使用 Node 24 运行时。
- Chrome 和 Edge 均完成本机实际运行验收；页面、日夜模式、总览按键、批量管理、操作教程和 API Key 开关正常，无页面脚本错误。
- 正文保存后的作品总字数、章节字数、今日字数、近六日数据和本地日期已统一；保存失败、并发保存、先删后恢复及草稿清理失败均不会造成错误累计。
- AI 检测、剧情锁定、AI 优化和消痕入口已验收；没有模型时会明确要求配置自己的工具模型，不会连接知屿后端。
- 全文分析已完成自备模型、本机检查点、分阶段恢复和八份资料保存验收；教程模拟过程不发起外部请求，也不写入真实作品。
- “知屿助手 / 内容反馈”已由用户使用自备模型完成实际页面验收，返回标题、正文和建议均为中文；共享 AI 输出层的自动回归同时覆盖思考标签、英文开场和后段英文正文拦截。

## 候选证据

- 社区运行包：186 个脚本、18 个样式，构建指纹 `488d44af21cc`。
- 自动门禁：`npm run verify:public` 全部通过，包含 5 个知屿内置模板、幂等初始化、用户模板保护、教程模板来源、本机模型转发的随机令牌与同源限制、地址/域名解析/跳转/敏感查询拒绝、实际连接固定到已检查地址、SSE 流式转发与取消、当前用户 Key 精确脱敏与密钥不落日志、OpenCode 正文兼容模型过滤、OpenAI/Anthropic 请求头、去重与恶意 ID 过滤、2 MiB 流式硬上限、跨域失败提示、同名模型安全覆盖、本地假模型双模式执行器、请求次数与达标停止、冻结用户模型、完成标记、不完整草稿、401 与空响应、真实错误日志、共享 AI 输出层中文约束与英文正文拦截、用户自备 API 边界、构建一致性、核心流程、全文分析、API Key 并发、旧 Key 升级、许可证、密钥和公开边界检查。
- 内置模板浏览器实测：模板页实际显示大纲 1 个、细纲 1 个、拆书 2 个、正文 1 个；分类筛选及大纲、细纲、拆书、正文的实际选择弹窗结果正确，详情作者显示“知屿写作”，重复启动后本机模板库仍为 5 份且没有副本，运行包指纹为 `7b9f34e73c46`，页面错误和控制台错误均为空。测试未调用模型。
- 浏览器保存实测：现有章节新增 1 字后，章节与作品总字数由 112 变为 113，今日字数由 1 变为 2，日期为 2026-08-24，运行包加载指纹与构建结果一致。
- 双模式浏览器实测：剧情/字数按钮可见并可相互切换，状态与 `aria-pressed` 同步；8000 字会显示“预计最多 6 次请求”并标记为需要确认；本地假模型执行器严格调用 1 次并移除剧情完成标记；普通日志与 401 错误均显示真实可操作文案。本次无外部模型请求，页面资源仅来自 `127.0.0.1:8081`，控制台无错误。
- 模型检索浏览器实测：本机假服务返回 3 条含重复记录后，下拉框显示 2 个唯一模型，可切换并保存；同名模型换 Key 和地址后保持单条记录，正文读取新 Key 与新地址；检索中点击取消会终止底层请求并清空提供商、URL、Key 和列表。404 与跨域失败会显示可操作原因并展开手动 ID；localhost HTTP 在用户地址许可和社区网络白名单下实际请求成功，页面无脚本错误。
- MiniMax 站点浏览器实测：添加模型弹窗可分别选择国内站和国际站；国内站自动填写 `https://api.minimaxi.com/v1`，国际站自动填写 `https://api.minimax.io/v1`，页面提示 Key 必须与申请站点对应，未填写或发送真实 Key。
- OpenCode 浏览器实测：选择 OpenCode Zen 后自动填写 `https://opencode.ai/zen/v1`；模型列表请求经 `127.0.0.1` 本机转发返回 200，下拉框实际显示 19 个正文接口兼容模型，包含当前官方 `chat/completions` 表中的 `x-preview-f-free`，并排除需要其他协议的 GPT、Claude、Gemini 及未登记接口的模型。测试只使用假 Key，未记住、未保存、未发起付费生成；页面控制台与脚本错误均为空。
- 浏览器：Chrome 与 Microsoft Edge 实际加载通过；Edge 验收环境标识为 `Edg/151`。
- 全文分析教程输出：`大纲.md`、`剧情总览.md`、`设定集.md`、`信息表.md`、`角色列表.md`、`追踪表.md`、`边界卡.md`、`承接卡.md`。
- 第三轮独立只读复核结果：P0=0、P1=0、P2=0；最后一项 Actions 可变标签 P3 已随后通过固定官方完整指纹关闭。
- 第四轮独立只读复核结果：P0=0、P1=0、P2=0、P3=1，结论为 GO；剩余 P3 仅建议以后把更多临时运行复测固化为自动回归测试，不影响当前候选功能和公开边界。
- 第五轮独立只读复核结果：P0=0、P1=0、P2=0、P3=1，结论为 GO；正文下方“保存章节”的社区版确认弹窗已恢复，编辑后立即保存、取消、确认、自动保存、快捷保存和历史版本均通过实际浏览器复测。剩余 P3 仅建议以后将该浏览器场景固化为自动回归测试。
- 本轮双模式独立只读复核结果：P0=0、P1=0、P2=0、P3=0，结论为 GO；请求次数上限与二次确认、假模型执行器回归、日志修复和更新记录均已复核关闭。
- 本轮用户自备 API 与 OpenCode 独立只读复核结果：P0=0、P1=0、P2=0、P3=0，结论为 GO；本机转发的 DNS 固定连接、私网与重定向拒绝、连接释放、Anthropic 请求头、SSE 取消、当前 Key 精确脱敏、OpenCode 正文兼容模型过滤和 IPv4/IPv6 localhost 兼容均已复核关闭。
- 本轮知屿内置模板独立只读复核结果：P0=0、P1=0、P2=0、P3=0，结论为 GO；五个模板的调用分类、用户模板保护、初始化幂等、教程名称、第三方残留清理和最终运行包自动检查均已复核关闭。

每次公开提交前必须运行：

```bash
npm test
npm run licenses:check
npm run secrets:check
npm run audit:public
```

本状态只适用于 `E:\桌面\知屿写作纯净源码\zhiyu-writing-community`。它不代表知屿线上版、原版电脑端或手机版已经修改、提交或发布。
