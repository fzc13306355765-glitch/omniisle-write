# 隐私说明

知屿写作社区版采用本地优先设计。

- 作品、章节、记忆库、模板和全文分析检查点保存在当前浏览器。
- 社区版不要求知屿账号，不连接知屿登录、云存储、计费或管理服务。
- 默认阻止外部网络请求。
- 使用 AI 前，软件会显示模型地址并要求用户确认。确认后，API Key、提示词、参考资料和正文会发送给该模型供应商。通过 `npm run serve` 本机运行时，请求会先经过仅监听 `127.0.0.1` 的本机转发；该转发不连接知屿服务器、不记录 API Key 或正文，也不会把 Key 放入网址。
- 新配置的 API Key 默认只保留在当前页面会话中，刷新或关闭页面后需要重新输入。用户可以主动开启“记住 API Key”，将密文保存在当前浏览器的 IndexedDB 中；该方式不能抵御已控制本机、恶意浏览器扩展或同源脚本，请勿在公共或共享设备上开启。
- 项目不内置统计和遥测上报。

本机转发的会话令牌只保存在当前页面内存中，用于阻止其他网页调用；它不能抵御已经控制当前电脑的恶意程序。用户仍应自行阅读所选模型供应商的隐私政策。清理浏览器数据、浏览器损坏或更换电脑可能导致本地作品丢失，请定期导出备份。
