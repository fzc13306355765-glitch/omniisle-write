# 使用支持

## 先自行检查

1. 使用 Node.js 20 或更高版本。
2. 运行 `npm ci`、`npm run build`、`npm run serve`。
3. 通过终端显示的本机地址打开，不要直接双击 HTML。
4. AI 无法调用时，核对模型地址、模型名、协议、Key 和供应商是否允许浏览器跨域请求。
5. 数据异常时先停止继续覆盖，并保留浏览器和导出文件的副本。

## 提交问题时

请提供操作系统、浏览器版本、项目版本、复现步骤和错误提示。使用虚构内容截图，并遮住 API Key、小说正文和账号信息。

社区维护者只支持本仓库代码。模型费用、限额、内容政策和服务可用性由用户选择的模型供应商负责。
